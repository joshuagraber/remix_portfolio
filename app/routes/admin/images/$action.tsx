export default function ImagesUpdater() {
	return (
		<div>
			{/* Upload images */}
			<div>
				<h3>Upload Images</h3>

				<h4>Images Uploaded</h4>
				{/* TODO: create component to display preview and url of image,
						keep all URLs in state array and use those to create component,
						map on each render to return. */}
			</div>

			{/* Update or delete images (depending on action param) */}
			<div>
				<h3>Update Images</h3>
			</div>
		</div>
	);
}
